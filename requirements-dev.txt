# Development dependencies for local testing with full features
#
# Install with: pip install -r requirements-dev.txt
#
# This includes heavy dependencies for optional server-side STL generation
# (does not work on Vercel, but useful for local development/testing)

# Production dependencies
-r requirements.txt

# Heavy geometry processing (optional - for local server-side generation)
numpy==2.5.1
trimesh==4.12.2
shapely==2.1.2
mapbox_earcut==2.0.0
scipy==1.18.0              # For mesh.fix_normals() (fallback: unify_normals)
matplotlib==3.11.1         # For character markers (fallback: rectangles)
manifold3d==3.5.2          # Boolean operations (not available on Vercel)

# Development tools
pytest==9.1.1
pytest-cov==7.1.0
ruff==0.16.0               # Linting and formatting
mypy==2.3.0               # Type checking
pre-commit==4.6.1          # Git hooks
